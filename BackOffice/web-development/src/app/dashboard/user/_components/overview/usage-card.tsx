import {Card, CardContent, CardHeader} from "@mui/material";
import React from "react";

export function UsageCard(): React.JSX.Element {
    return (
        <Card sx={{height: '100%'}}>
            <CardHeader title={'App Usage'}/>
            <CardContent>

            </CardContent>
        </Card>
    )
}